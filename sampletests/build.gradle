apply plugin: 'java'
apply plugin: 'alfresco-dynamic-extension'

dependencies {
    compile project(':testrunner')
}

jar {
    manifest {
        instruction 'Bundle-Description', 'Integration testrunner sample'
    }
}

